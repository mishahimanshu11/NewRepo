name: Publish2 🚀

on:
  workflow_dispatch:
  push:
    branches:
    - feature


jobs:
  publish:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '6.0.x'
          
      - name: Restore
        run: dotnet restore ./ASPWebapplication.sln
        
      - name: Build
        run: dotnet build .ASPWebapplication.sln --configuration Release --no-restore
        
      - name: Publish
        run: dotnet publish .ASPWebapplication.sln --configuration Release --no-build 
